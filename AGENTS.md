# Project Agent Rules

## Test Gate (Mandatory)
- Any code change must be followed by running `swift test` in `the repository root`.
- If tests fail, fix the implementation and rerun `swift test` until all tests pass.
- Do not finish a task report for code changes while tests are failing.

