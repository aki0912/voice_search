# voice_search ロードマップ

## フェーズ1: コアロジックの確定（完了）
- テキスト正規化
- キーワード/フレーズ検索
- ユーザー辞書（エイリアス）
- 時間→トークン探索

## フェーズ2: 文字起こし基盤（完了）
- `TranscriptionService` 抽象層を追加
- URLベース文字起こしアダプタを導入（現状は `SFSpeechURLRecognitionRequest`）
- 非同期解析結果を `TranscriptWord` に収束
- 拡張子検証と結果サニタイズをパイプライン化

## フェーズ3: 再生・検索UI（完了）
- ドラッグ&ドロップ実装
- 検索画面 + 検索結果のシーク
- 再生位置ハイライト（近傍単語）

## フェーズ4: 品質改善（進行中）
- 辞書登録UI・永続化（完了）
- `SpeechAnalyzer` 連携アダプタへの置換
- 長尺/複数ファイルのジョブ管理
- エラーハンドリングと再試行
- パフォーマンス計測（メモリ・進捗）

## 進捗補足（追加）
- アプリ実行可能ターゲット `VoiceSearchApp` を追加し、コアに接続済み。
- 次の最優先は SpeechAnalyzer API 対応か、SFSpeechの再試行・例外分岐追加。
